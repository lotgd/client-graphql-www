query getCharactersForUser($userId: String!) {
    user(id: $userId) {
        characters {
            ...CharacterFragments
        }
    }
}

fragment CharacterFragments on Character {
    id
    name
    displayName
}